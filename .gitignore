.direnv
.pre-commit-config.yaml
.DS_Store
.hugo_build.lock
.netlify
.pnpm*
.tmp
.venv
.zig-cache
*.core
*.err
*.err
*.log
*.pdf
static/pdfs/
*.tmp.html
.pp_*.typ
**/internal/*
**/node_modules/*
**/public/*
# stage-site (#173) default staging dir; CI passes an explicit temp dir
.pp-stage/
hugo_stats.json
resources
tmp.*
zig-out
zig-pkg
.claude
starter/templates/
starter/static/*
# …but ship the turnkey deploy config the starter references (SSO gate +
# security headers) and a neutral placeholder logo, so a fresh "Use this
# template" repo builds a complete site on the first push; the user replaces
# static/logo.png with their own brand mark.
!starter/static/staticwebapp.config.json
!starter/static/_headers
!starter/static/logo.png
starter/public/
starter/starter/
sbom.cdx.json
sbom.spdx.json
sbom.csv
http_cache.sqlite

# CI attestation evidence (generated; uploaded as an artifact, never committed)
evidence/
